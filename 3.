#include <stdio.h>

int main() {
    int numero;

    printf("Digite um número inteiro: ");
    scanf("%d", &numero);

    if (numero % 3 == 0) {
        printf("O número é múltiplo de 3.\n");
    } else {
        printf("O número não é múltiplo de 3.\n");
    }

    return 0;
}
